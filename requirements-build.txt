# WeRDeep Build Dependencies
# Pinned versions for reproducible builds

# Compiler and build tools
cython>=3.0

# Quality checks
ruff>=0.3.0
mypy>=1.8.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Runtime dependencies (needed during build for imports)
scrapy>=2.11.0
jsonschema>=4.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.28.0
